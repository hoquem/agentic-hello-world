# This app talks to your LOCAL Ollama daemon, which proxies cloud models.
# One-time setup:  ollama signin   &&   ollama pull kimi-k2.6:cloud
# No API key here — the daemon holds the Ollama Cloud credential.
OLLAMA_HOST=http://localhost:11434
MODEL=kimi-k2.6:cloud
